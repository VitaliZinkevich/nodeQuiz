// node modules
import {combineReducers} from 'redux'

// state holdings in combine reducers

const rootReducers = combineReducers ({
    state: (state = {}) => state
    })

export default rootReducers
